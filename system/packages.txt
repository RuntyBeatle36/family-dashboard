# apt packages needed on the Pi, one per line. Blank lines and lines
# starting with # are ignored. Installed by scripts/system-update.sh on
# every update (a no-op if everything listed is already present).
#
# Piper (TTS) is NOT here — it's a standalone binary + voice model, not an
# apt package. See scripts/provision-pi.sh / the README's Text-to-speech
# section. Add entries here for future needs (e.g. Bluetooth/audio packages).

unclutter
